# .env.example
# SQL_DATABASE can be a local filename (SQLite) or a postgresql:// URL (PostgreSQL)
SQL_DATABASE=arcadia.db
# VITE_API_URL should point to your Express server (usually empty in development as it uses the current origin)
VITE_API_URL=
